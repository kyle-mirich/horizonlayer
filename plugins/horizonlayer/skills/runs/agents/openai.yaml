interface:
  display_name: "HorizonLayer Runs"
  short_description: "Resume agent work with sessions and checkpoints"
  default_prompt: "Use $runs to resume this agent task and checkpoint meaningful progress."
dependencies:
  tools:
    - type: "mcp"
      value: "horizonlayer"
      description: "Resumable sessions and execution checkpoints in HorizonLayer"
policy:
  allow_implicit_invocation: true
